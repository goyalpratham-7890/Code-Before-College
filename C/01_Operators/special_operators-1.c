#include<stdio.h>
int main()
{
    int a;
    a=(printf("Jenny\n",7));
    printf("%d",a);
}
/* Here , separates the 7 and its been declared as extra argument
there and as output we get count of Jenny+1(\n) as 6 */
